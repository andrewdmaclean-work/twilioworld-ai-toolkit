# Local AI model weights — downloaded by toolkit setup, never committed (multi-GB)
models/*.gguf
models/*.bin
models/*.tar.gz
models/*.download
models/*.log
models/extract_tmp/
models/voice/
# llamafile runtime binary — downloaded by toolkit setup
tools/llamafile
tools/llamafile.exe
tools/whisperfile
tools/whisperfile.exe

# Secrets / local env
.env
.env.local
.toolkit/config.json
.toolkit/pi-agent/
.pi/auth.json
.pi/*cache*.json
.pi/sessions/

# OS noise
.DS_Store
models/system-prompt.txt

# OpenTUI dashboard prototype (tui/) — Bun-managed
tui/node_modules/
tui/bun.lock
tui/bun.lockb
